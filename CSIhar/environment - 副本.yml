name: csi-har
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1      # 如果没有 NVIDIA GPU，可删除本行并添加 cpuonly
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - seaborn
  - pillow
  - tqdm
  - pyyaml
  - opencv
  - pip
  - pip:
      - rich
      - thop
