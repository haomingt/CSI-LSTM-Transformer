seed: 2024

data:
  raw_root: ../data_raw_1
  processed_root: ../data_processed
  use_offline_images: false
  classes: ["bend","fall","lie_down","run","sitdown","standup","walk"]
  file_ext: ".csv"
  max_time_len: 1100
  min_time_len: 200
  subcarriers: 52
  resize: [64, 64]
  cmap: "viridis"
  norm_mode: "per_sample"    # per_sample / global
  cache_in_memory: true
  train_split: 0.75
  val_split: 0.10
  test_split: 0.15
  # 新增的小波变换配置
  use_wavelet: false
  wavelet_type: "haar"
  wavelet_level: 4
  # 新增的zscore归一化配置
  use_zscore: false

training:
  epochs: 120
  batch_size: 32
  num_workers: 4
  lr: 1e-3
  weight_decay: 1e-4
  optimizer: adamw           # 不加引号也可以
  scheduler: cosine          # cosine / step / none
  step_size: 30
  gamma: 0.1
  warmup_epochs: 5
  amp: true
  grad_clip: null            # 或 1.0
  early_stop_patience: 25

model:
  name: deeper_cnn
  in_channels: 3
  dropout: 0.25

evaluation:
  topk: [1, 3]

logging:
  out_dir: ./outputs
  tqdm: true
  save_interval: 20